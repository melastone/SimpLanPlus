{
    /*
    This example shows the use of nested functions.
    The program is should output an error.
    */

    void f(int a) {
        void g(int b) {
            print b;
        }

        g(a);
    }

    f(2);
}
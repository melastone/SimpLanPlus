{
    /*
    Questo file possiamo usarlo per testare codice al volo.
    */

    int a = 1;

    void f(var int x) {
        int k = 1;
        print x;
    }

    f(a);

    /*
    n == 3
        a -> BOTTOM
        n -> USED

    n == 2
        a -> BOTTOM
        n -> USED

    n == 1
        a -> BOTTOM
        n -> USED

    n == 0
        a -> INIT
        n -> USED
    */
}
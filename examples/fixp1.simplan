{
    /*
        This example shows the result of Effect Analysis.
        It contains a recursive function, for which we need to use the fixed point algorithm.
        Effect Analysis should end successfully: any variable is in an error status.
        Fixed point will be [n -> used]
    */

    int x = 6;

    int fact(int n) {
        if (n==0){
            return 1;
        } else {
            return n * fact(n-1);
        }
    }
    fact(x);
}
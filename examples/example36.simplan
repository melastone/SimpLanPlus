{
    /*
    This example contains lexical and syntactic error. The token # cannot be recognized. The variable declaration is missind the ID.
    */

    int # = 1;
    if (x >= 0)
        print x;
    else
        return;
}
{
    /*
        This example shows what happens with if statements if we do not perform Effect Analysis.
        The program should output a semantic error: we cannot increment a when the if statement is performed.
    */

    int x; int n = 1;
    if (n > 0) { print n;} else { x = 0; print x;}
    x = x + 1;
}